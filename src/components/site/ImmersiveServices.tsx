import { useEffect, useMemo, useState } from "react";
import { ArrowRight, ArrowUpRight, Sparkles } from "lucide-react";
import { Link } from "@tanstack/react-router";
import { useLang } from "@/lib/i18n";
import { SERVICES } from "@/lib/content";
import { UI } from "@/lib/copy";
import { cn } from "@/lib/utils";
import { ServiceIllustration } from "./ServiceIllustration";

const ACTIVE_IDS = ["websites", "branding", "seo", "maps", "social", "maintenance", "ecommerce"];
const SERVICE_NUM: Record<string, string> = { websites: "01", branding: "02", seo: "03", maps: "04", social: "05", maintenance: "06", ecommerce: "07" };

export function ImmersiveServices() {
  const { t, price } = useLang();
  const services = useMemo(() => ACTIVE_IDS.map((id) => SERVICES.find((service) => service.id === id)).filter(Boolean) as typeof SERVICES, []);
  const [active, setActive] = useState(0); const [progress, setProgress] = useState(0);
  useEffect(() => { const section = document.getElementById("xr-services-immersive"); if (!section) return; let frame = 0; const update = () => { frame = 0; const rect = section.getBoundingClientRect(); const total = Math.max(section.offsetHeight - window.innerHeight, 1); const value = Math.min(1, Math.max(0, -rect.top / total)); setProgress(value); setActive(Math.min(services.length - 1, Math.floor(value * services.length))); }; const onScroll = () => { if (!frame) frame = window.requestAnimationFrame(update); }; update(); window.addEventListener("scroll", onScroll, { passive: true }); window.addEventListener("resize", onScroll); return () => { window.removeEventListener("scroll", onScroll); window.removeEventListener("resize", onScroll); if (frame) window.cancelAnimationFrame(frame); }; }, [services.length]);
  const activeService = services[active] ?? services[0];
  const hrefFor = (id: string) => id === "maintenance" ? "/services/webcare" : `/services/${id}`;
  return <section id="xr-services-immersive" className="relative bg-background"><header className="mx-auto max-w-[1600px] px-5 pb-12 sm:px-8 lg:px-12 lg:pb-16"><div className="border-y border-border/60 py-8 lg:py-10"><div className="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between"><div className="max-w-5xl"><p className="label-mono text-[10px] uppercase tracking-[0.28em] text-primary">{t(UI.servicesLabel)} · 07</p><h1 className="display-serif mt-4 text-[clamp(3rem,7vw,7.5rem)] leading-[0.84]">Des expertises à <em className="text-primary not-italic italic">explorer</em>.</h1></div><p className="max-w-md text-sm leading-7 text-muted-foreground">Faites défiler pour traverser nos sept expertises. Une seule scène, une expertise à la fois, sans cartes empilées ni rupture de lecture.</p></div></div></header><div className="relative" style={{ height: `${Math.max(services.length * 82, 100)}svh` }}><div className="sticky top-0 flex h-[100svh] min-h-[680px] items-center overflow-hidden py-8 lg:py-12"><div className="mx-auto w-full max-w-[1600px] px-5 sm:px-8 lg:px-12"><div className="grid items-center gap-8 lg:grid-cols-[220px_minmax(0,1fr)] xl:grid-cols-[280px_minmax(0,1fr)]"><aside className="hidden lg:block"><p className="label-mono text-[9px] uppercase tracking-[0.25em] text-muted-foreground">XR / SERVICES</p><div className="mt-5 flex items-baseline gap-2"><span className="display-serif text-7xl text-primary tabular-nums">{String(active + 1).padStart(2, "0")}</span><span className="label-mono text-xs text-muted-foreground">/ 07</span></div><div className="mt-8 h-36 w-px bg-border"><div className="w-px bg-primary transition-[height] duration-300" style={{ height: `${progress * 100}%` }} /></div><div className="mt-8 space-y-2">{services.map((service, index) => <div key={service.id} className={cn("label-mono text-[9px] uppercase tracking-[0.12em] transition-all duration-300", index === active ? "translate-x-2 text-foreground" : "text-muted-foreground/35")}>{SERVICE_NUM[service.id]} · {t(service.title)}</div>)}</div></aside><div className="relative min-h-[620px] lg:min-h-[680px]">{services.map((service, index) => { const distance = index - active; const visible = Math.abs(distance) <= 1; return <article key={service.id} aria-hidden={index !== active} className="absolute inset-0 transition-[opacity,transform] duration-700 ease-[cubic-bezier(.22,1,.36,1)]" style={{ opacity: visible ? (index === active ? 1 : 0.18) : 0, transform: `translate3d(${distance * 3.5}%, ${distance * 2.5}%, 0) scale(${index === active ? 1 : 0.96})`, zIndex: index === active ? 3 : 1, pointerEvents: index === active ? "auto" : "none" }}><div className="grid h-full overflow-hidden rounded-[1.75rem] border border-border/70 bg-card/70 shadow-2xl backdrop-blur-sm lg:grid-cols-[1.15fr_0.85fr]"><div className="relative min-h-[280px] overflow-hidden lg:min-h-full"><ServiceIllustration service={service.id} title={t(service.title)} /><div className="absolute inset-0 bg-black/25" /><div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/10 to-transparent" /><div className="absolute left-5 top-5 rounded-full border border-white/20 bg-black/30 px-3 py-1.5 backdrop-blur-md"><span className="label-mono text-[9px] uppercase tracking-[0.18em] text-white">XR AGENCY · {SERVICE_NUM[service.id]}</span></div><div className="absolute bottom-5 left-5 right-5 flex items-end justify-between gap-4 text-white sm:bottom-7 sm:left-7 sm:right-7"><div><span className="label-mono text-[9px] uppercase tracking-widest text-white/60">À partir de</span><div className="display-serif mt-1 text-3xl sm:text-4xl">{price(service.fromEur)} <span className="label-mono text-[9px] text-white/60">{service.fromPeriod === "month" ? "/ mois" : service.fromPeriod === "year" ? "/ an" : ""}</span></div></div><Sparkles className="hidden h-5 w-5 text-white/70 sm:block" /></div></div><div className="flex flex-col justify-between p-6 sm:p-9 lg:p-11"><div><div className="flex items-center justify-between"><span className="label-mono text-[9px] uppercase tracking-[0.22em] text-primary">{t(service.short)}</span><span className="label-mono text-[9px] text-muted-foreground">{String(index + 1).padStart(2, "0")} / 07</span></div><h2 className="display-serif mt-6 text-4xl leading-[0.9] sm:text-5xl xl:text-6xl">{t(service.title)}</h2><p className="mt-5 max-w-xl text-sm leading-7 text-muted-foreground">{t(service.description)}</p><div className="mt-7 grid gap-2 sm:grid-cols-2">{service.highlights.slice(0, 4).map((highlight) => <div key={highlight.fr} className="rounded-xl border border-border/70 bg-background/50 px-3 py-2.5 text-xs text-foreground">{t(highlight)}</div>)}</div></div><div className="mt-8 border-t border-border/70 pt-6"><div className="mb-4 flex items-center gap-3"><div className="h-px flex-1 bg-border" /><span className="label-mono text-[9px] uppercase tracking-[0.15em] text-muted-foreground">{service.plans.length} formules</span></div><a href={hrefFor(service.id)} className="group inline-flex items-center gap-3 rounded-full bg-primary px-6 py-3.5 text-xs font-semibold uppercase tracking-widest text-primary-foreground transition-all hover:gap-5">Découvrir la prestation <ArrowRight className="h-4 w-4" /></a></div></div></div></article>; })}</div></div><div className="mt-5 flex items-center justify-between lg:hidden"><span className="label-mono text-[9px] uppercase tracking-[0.18em] text-muted-foreground">{String(active + 1).padStart(2, "0")} / 07 · {t(activeService?.title ?? services[0].title)}</span><span className="label-mono text-[9px] uppercase tracking-[0.18em] text-primary">Scroll ↓</span></div></div></div></div><div className="mx-auto max-w-[1600px] px-5 pb-20 sm:px-8 lg:px-12 lg:pb-28"><div className="rounded-[1.75rem] border border-primary/20 bg-primary/[0.04] p-7 sm:p-10 lg:p-14"><div className="flex flex-col gap-7 lg:flex-row lg:items-end lg:justify-between"><div><p className="label-mono text-[10px] uppercase tracking-[0.22em] text-primary">XR Intelligence</p><h2 className="display-serif mt-3 text-3xl sm:text-5xl">Besoin d'un parcours sur mesure ?</h2><p className="mt-4 max-w-2xl text-sm leading-7 text-muted-foreground">Notre configurateur analyse votre situation et construit une recommandation adaptée.</p></div><Link to="/" hash="quote" className="inline-flex shrink-0 items-center gap-2 text-xs font-semibold uppercase tracking-widest text-primary hover:underline">Lancer XR Intelligence <ArrowUpRight className="h-4 w-4" /></Link></div></div></div></section>;
}
